<!DOCTYPE html>
<html>
<head>
    <title>Livres par Auteur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Livres triés par Auteur</h1>

        <a href="{{ path('app_books') }}" class="btn btn-secondary mb-3">Retour à la liste principale</a>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Publication Date</th>
                    <th>Published</th>
                    <th>Category</th>
                    <th>Author</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for book in books %}
                <tr>
                    <td>{{ book.title }}</td>
                    <td>{{ book.publicationDate|date('F d, Y') }}</td>
                    <td>{{ book.published ? 'Yes' : 'No' }}</td>
                    <td>{{ book.category }}</td>
                    <td>{{ book.author.username }}</td>
                    <td>
                        <a href="{{ path('book_show', {'id': book.id}) }}" class="btn btn-info btn-sm">show</a>
                        <a href="{{ path('book_edit', {'id': book.id}) }}" class="btn btn-warning btn-sm">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="text-center">Aucun livre trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>