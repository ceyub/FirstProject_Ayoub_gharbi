<!DOCTYPE html>
<html>
<head>
    <title>Détails du Livre</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Détails du Livre</h1>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ book.title }}</h5>
                <p class="card-text">
                    <strong>Référence:</strong> {{ book.ref }}<br>
                    <strong>Catégorie:</strong> {{ book.category }}<br>
                    <strong>Date de Publication:</strong> {{ book.publicationDate|date('d/m/Y') }}<br>
                    <strong>Publié:</strong> {{ book.published ? 'Oui' : 'Non' }}<br>
                    <strong>Auteur:</strong> {{ book.author.username }}<br>
                    <strong>Email de l'auteur:</strong> {{ book.author.email }}
                </p>
                <a href="{{ path('app_books') }}" class="btn btn-primary">Retour à la liste</a>
                <a href="{{ path('book_edit', {'id': book.id}) }}" class="btn btn-warning">Modifier</a>
            </div>
        </div>
    </div>
</body>
</html>