{% extends 'base.html.twig' %}

{% block title %}Reader index{% endblock %}

{% block body %}
    <h1>Reader index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Membership_date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reader in readers %}
            <tr>
                <td>{{ reader.id }}</td>
                <td>{{ reader.name }}</td>
                <td>{{ reader.email }}</td>
                <td>{{ reader.membershipDate ? reader.membershipDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_reader_show', {'id': reader.id}) }}">show</a>
                    <a href="{{ path('app_reader_edit', {'id': reader.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reader_new') }}">Create new</a>
{% endblock %}
